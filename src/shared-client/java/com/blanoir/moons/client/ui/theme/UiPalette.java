package com.blanoir.moons.client.ui.theme;

/** Semantic colors shared by ClickGUI and HUD renderers. */
public record UiPalette(
        int window,
        int header,
        int panel,
        int row,
        int rowHover,
        int selected,
        int border,
        int primary,
        int primaryLight,
        int primaryDark,
        int textPrimary,
        int textSecondary,
        int textDisabled,
        int controlTrack,
        int field
) {
}
